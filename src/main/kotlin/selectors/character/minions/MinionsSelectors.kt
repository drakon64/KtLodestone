package cloud.drakon.ktlodestone.selectors.character.minions

internal object MinionsSelectors {
    const val ROOT = ".minion__list__item"

    const val NAME = ".minion__name"

    const val TOTAL = ".minion__sort__total > span:nth-child(1)"
}
